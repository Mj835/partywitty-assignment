export type PartyProfile = {
  name: string
  age: number
  mutuals: string
  image: string
  tags: readonly string[]
  badge: string
}

export type VerificationStep = {
  title: string
  description: string
}
